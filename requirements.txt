# NexaQuant — dev / research dependencies (kitchen-sink superset).
# Slim runtime subsets live in `requirements-live.txt` and `requirements-dashboard.txt`.
#
# Alphabetized + deduplicated by ENG001. Every entry that was present before is
# still here — no dependency removed. Trailing-space duplicates have been merged.

aiohttp
arch
asyncio
copulas
dash
featuretools
great_expectations
hmmlearn
hydra-core
langchain
lime
MetaTrader5
networkx
numpy
omegaconf
pandas
plotly
pyyaml
ray[rllib]
retrying
river
scikit-learn
scipy
shap
stable-baselines3[extra]
statsmodels
ta
torch
torchaudio
torchvision
transformers
tsfresh
