# Minimal, Linux-safe deps for deploying india/aegis_dashboard.py to a cloud host.
# Deliberately does NOT include the dev kitchen-sink (torch, ray, transformers,
# MetaTrader5 — which is Windows-only and BREAKS any Linux cloud build).
streamlit>=1.30
altair>=5.0
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1
pyarrow>=14.0
