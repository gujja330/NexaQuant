# NexaQuant — Session Log (2026-06-18)

Continuation. Focus: strategy backlog build-out, multi-edge portfolio, lot-sizing, the
deploy saga (MT5/Wine hit a wall), and the pivot to CCXT for free crypto paper trading.

## WHERE WE LEFT OFF (resume here)
**Waiting on YOU:** register a free **Bybit testnet** account + create API keys, then launch
the CCXT bot. Everything is built and pushed.

Resume steps (on the VM):
```bash
cd /home/ubuntu/NexaQuant && git pull
python3 -m pip install --user ccxt
export CCXT_API_KEY='your_testnet_key'
export CCXT_SECRET='your_testnet_secret'
python3 execution/ccxt_trader.py --mode paper --poll 60
```
Success = `connected: bybit TESTNET balance=... symbols=['BTC/USDT:USDT'] edges=['trend','breakout'] (2 sleeves)`.
Then I wire it as a systemd service for autonomous 24/7 paper -> 30-day track record begins.
Testnet signup: https://testnet.bybit.com  (profile -> API -> Create New Key -> Read+Trade).

## What we built today
### Strategy backlog (research-sourced, each gated)
- ADOPTED: **lengthy-candle size boost** (gold/BTC), **multi-lookback TSM** (gold only,
  Sharpe -0.14->+0.55), **fundamental macro gate** (gold, +TSM+macro Sharpe ->0.68).
- REJECTED (tested, failed gate): pyramiding, candle streaks, fast TFs (M5/M15), vol-target
  overlay, crash-protect, meta-label-as-AI-sizer (AUC~0.5 no skill), mean-reversion sleeve,
  cross-sectional momentum (82% DD — 4 cryptos isn't a real cross-sectional universe).
- VALIDATED 2nd edge: **volatility breakout (Donchian)** on BTC+gold — low corr with trend
  (+0.30/+0.15) => real diversifier. (strategy/breakout.py)

### Multi-edge PORTFOLIO (the consistency engine)
- strategy/portfolio.py: equal-risk (inverse-vol) blend of (edge x instrument) sleeves.
- Result: **+34.2%/5y vs +24.3% single-edge; +10.8%/3y; 6/6 profitable years; maxDD 5.3%**;
  cross-sleeve corr ~0.0-0.3. (research/portfolio_results.py)
- Deep cross-asset test (BTC/gold/SPX/EURUSD/WTI daily): **S&P trend is a real uncorrelated
  edge** (Sharpe 1.59/99y); FX/oil-trend dead. Naive breadth dilutes -> use a per-sleeve
  quality gate. Daily breadth = robustness but lower return; best engine stays BTC+gold H4.
  (research/multi_asset_portfolio.py)

### Live bot upgraded
- live_trader now trades the FULL multi-edge portfolio: each (symbol x edge) is a sleeve with
  its own broker position (magic number); MultiBot builds sleeves from config (live_symbols x
  live_edges); ONE terminal, ONE account-wide RiskManager.
- Sizing: `_risk_frac(conf)=min(base% x conf, 2%)` used for BOTH lot and the 6% portfolio cap.
- Netting/hedging auto-detect: on a netting account, falls back to one edge/symbol (safe).
- Config fully drives the universe (system.live_symbols / live_edges / live_tf).

### Lot sizing (confirmed dynamic, Option B)
- `_calc_lots`: lot = (live_balance x risk% x confidence) / broker_loss_per_lot, read live;
  rounded to volume step; tiny-account feasibility SKIP. `_refresh_equity` re-reads balance
  each cycle (compounds/withdraws). $10 study: fixed lots ruin a standard acct / huge DD on
  cent; compounding %-risk is the safe winner. (research/lot_size_sim.py)

## The deploy saga (Oracle 1GB VM) — and the honest verdict
- setup_oracle.sh hardened repeatedly: embeddable Python (headless), timeouts on all Wine
  calls, WineHQ install (wine-11.0), win64 prefix init + health check.
- Fixed many issues: fetestexcept crash (old Wine) -> WineHQ; kernel32 c0000135 -> prefix
  init needs xvfb + correct WINEARCH (user typo'd WINSEARCH once); kernel32.dll finally built.
- **BUT MT5 silent install hung 30+ min. Verdict: 1GB RAM is too small for MT5+Wine 11**
  (wineboot services all failed to start). Not fixable by config. Exness/MT5 code is kept
  intact for a future proper host (~$5/mo VPS or a PC left on).

## The pivot: CCXT (native crypto, NO Wine)
- execution/ccxt_trader.py: same multi-edge engine (trend+breakout, long+short) on a crypto
  exchange via CCXT. Pure-Python -> runs on the 1GB box. Sleeves + shared RiskManager,
  dynamic risk sizing, software-managed scale-out/momentum/stop, sandbox(testnet) default.
  Config block `ccxt` (exchange=bybit, testnet=true, swap, BTC/USDT:USDT, edges trend+breakout).
  Validated in dry-run (both sleeves run).
- HONEST: CCXT works with crypto EXCHANGES only — **NOT Exness/MT5** (different protocol).
  CCXT path = new free testnet account, crypto only, NO gold. Exness needs MT5 (a real host).

## Honest scorecard
~7.5-8/10 engine: diversified, validated (BTC+gold+S&P edges), config-driven, safe. The last
~2 points need: paid intraday multi-market data + more edge types + a LIVE track record (time)
-- not codeable in one pass. Realistic ceiling: beats a bank several-fold at low drawdown,
compounding over years. NOT 2%/month.

## Pushed commits (today)
backlog/portfolio/breakout/cross-sectional, multi-asset gate, live multi-edge bot, risk_frac,
netting guard, CCXT adapter. Repo: github.com/praveen330/NexaQuant (main, all pushed).

## Next steps (when you return)
1. Bybit testnet keys -> launch ccxt_trader (steps at top).
2. I wire the CCXT systemd service (autonomous 24/7).
3. 30-day paper -> the live track record begins.
4. (Later) gold/Exness on a proper MT5 host; more edges/data toward higher score.
