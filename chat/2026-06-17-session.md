# NexaQuant — Build & Deploy Session Log (2026-06-17)

Reference log of the end-to-end session: from "what is this folder" to a deployed cloud bot.

## What we started with
- A half-built "marl" multi-agent RL trading repo (XAUUSD H1/H4/D1 only), validation FAILED,
  a plaintext broker password in config, and 3 source PDFs (MARL architecture, 75-80% win-rate
  survey, Neural Commerce).

## What we did (chronological highlights)
1. **Honest edge probe** on gold: trend-continuation has a real, cost-robust edge; mean-reversion
   loses; win-rate is a trap (expectancy/payoff matter). Most absolute return was gold beta.
2. **Cleaned + renamed** the project to **NexaQuant**; archived old scaffolding to `legacy/`;
   removed the plaintext password (env vars); built a clean structure (config/ data/ strategy/
   backtest/ research/ execution/ docs/).
3. **SMC stack** (FVG, BOS/CHoCH, order blocks, liquidity sweeps, premium/discount): only the
   CONTINUATION pillars (structure+FVG) have edge; mean-reversion pillars lose in trends;
   stacking all 5 ("A+") LOST money -> a REGIME GATE decides which pillars are active.
4. **Regime gate**: rule-based ADX + a learned causal **HMM** (HMM beat ADX on H1).
5. **Exit management** (the profit driver): hard ATR stop + **momentum-ride** (no fixed TP) +
   scale-out. Tight trailing HURTS; momentum-ride wins.
6. **AI layer**: meta-labeling (HistGBM/ensemble, calibrated) + confidence sizing + FinBERT slot.
   Honest: AUC ~0.5 now (needs more data/features); validation-AI (CPCV/Deflated Sharpe/PBO) is
   where AI helps MOST — it flagged BTC H1 as overfit.
7. **Deep research** (multi-agent, cited): validated stops-are-regime-conditional, no-fixed-TP,
   CPCV/DSR, SMC has no independent academic edge, defer RL/DL. Added to the architecture deck.
8. **Free data**: fixed pullers; pulled 5 instruments 2021-2026 (Binance) + deep daily via
   yfinance (gold 2000+, BTC 2014+, SPX 1927+).
9. **Multi-TF / multi-pair / multi-year walk-forward** (the reality check):
   - Best timeframe = **H4** (not lower; lower TFs add noise + cost).
   - **Long-only loses in bear years**; **regime-aware LONG+SHORT fixes it** -> BTC **H4 profitable
     5/5 years incl. 2022 bear**.
   - **Confidence-scaled sizing** (by trend strength) took BTC H4 +50% -> +117%, Sharpe 2.0 -> 2.4.
   - DEEP daily across decades: edge is **regime-dependent, not all-weather**; works on trending
     assets (BTC, gold) in trending regimes; FX/oil/equities-daily = no edge. Manage expectations.
10. **MT5 live bot** (`execution/live_trader.py`): regime-aware long+short, confidence sizing,
    ATR stop + momentum-ride + scale-out, kill switch, Telegram/email alerts, broker-accurate
    dynamic lot sizing, min-lot feasibility guard, `--check` pre-flight, `--mode dry-run|paper|live`.
11. **Deployment**: chose **Oracle Cloud Always-Free** VM (free; card for verification only).
    `setup_oracle.sh` automates Wine+MT5+Python+systemd. Also `setup_windows.ps1` for a home PC.

## Validated configuration (current best)
- Instruments: **BTC + XAU** (both primary). Timeframe: **H4**.
- Regime-aware **long+short**, momentum-ride exit + scale-out, hard ATR stop, confidence sizing.
- BTC H4 = lead config (5/5 years, PBO 0.22). NOT live-ready until 30-day paper passes.

## Account / infra (this session)
- Exness **Standard Cent** account ~**1196 USC (~$12)** for paper/micro live testing.
- Oracle VM **`nexabot`** (Ubuntu 22.04, E2.1.Micro Always-Free), public IP **140.245.245.134**,
  SSH as `ubuntu` with key. Connected successfully.

## Honest status
- System is complete, dynamic (any broker/account/instrument), and stress-tested.
- The edge is real but **regime-dependent** — paper trade BTC H4 first; do NOT expect a money machine.
- Pending: push to GitHub -> clone on VM -> run setup_oracle.sh -> 30-day paper -> (maybe) live.

## Next steps
1. Push repo to github.com/praveen330/NexaQuant.
2. On VM: git clone -> `bash execution/setup_oracle.sh BTCUSDm H4 paper` (with MT5 creds in env).
3. `--check` to confirm lot sizing on the cent account, then run paper ~30 days.
4. Deepen gold history (HistData/Dukascopy) for gold multi-year walk-forward.

---

# Continuation Session (2026-06-17, later) — Pip-capture research + self-learning loop

## User goal this round
Capture more pips (long & short); test the user's market intuitions; make the model
self-learning week-by-week; confirm auto lot sizing + compounding; deploy on Oracle VM.

## Ideas tested under the rigor gate (BTC H4, long+short, tiered risk, net of cost)
| Idea | Verdict | Evidence |
|------|---------|----------|
| **Pyramiding** (add-to-winner) | ❌ REJECTED | +42% → -13%, drawdown 4% → 20% (adds buy near tops) |
| **Lengthy-candle gate** (body≥1.5×ATR as FILTER) | ✅ strong | 52% win, PF 3.38, 1,444 avg pips vs 84 baseline |
| **Lengthy-candle as SIZE BOOST** (+50%) | ✅ **ADOPTED** | H4 +42% → **+54%** at SAME 4% drawdown |
| **Candle streaks 2/3/4 as entry** | ❌ REJECTED | PF < 1.0 on EVERY timeframe (patterns fire too late) |
| **Pure expansion-candle entry** | ❌ REJECTED | PF 0.96 |
| **Fast timeframes M5/M15** | ❌ REJECTED | M5 = -100% (wiped), M15 = -77% (cost + noise) |

**Principle confirmed:** candle features as a FILTER/size-boost on the validated trend
edge = win; candle patterns as a standalone TRIGGER = lose (they're late).

New probes added: `research/pyramid_test.py`, `research/expansion_test.py`, `research/streak_test.py`.
Pulled BTC **M5 (569k bars)** + **M15 (190k bars)** from Binance 2021-2026.

## Self-learning weekly loop — BUILT (`execution/auto_update.py`)
1. PULL fresh bars incrementally (history grows week-by-week, dedup merge).
2. RE-VALIDATE via anchored **per-year walk-forward** (fixed the earlier single-slice bug
   that understated H4 — verified BTC H4 = **5/5 yrs profitable, avg 6.7%/yr, worst +1.7%**).
3. PROMOTE config only if it passes gate AND beats champion (`champion.json`) — never auto-degrades.
4. Write `health.json` = the bot's **TRADING LICENSE**. `live_trader._trading_license()` reads it
   each cycle and **auto-stands-down to manage-only** if the edge stops persisting or the check
   goes stale (>21d). Self-learning WITH a guardrail (can't trade a dead edge).
5. Wired as a **systemd timer** in `setup_oracle.sh` (`nexa-update.timer`, weekly Mon 02:00 + on-boot).

## Auto lot sizing + compounding — CONFIRMED already built
- `_calc_lots()`: broker-accurate via `symbol_info` (works any symbol, cent/standard), confidence-
  scaled (incl. lengthy-candle boost), rounds to volume step, tiny-account feasibility guard.
- `_refresh_equity()` (each cycle, when flat): re-reads live balance → lots auto-grow on compounding,
  auto-shrink on withdrawal/drawdown; alerts on >2% balance change. ZERO hardcoding.

## Architecture deck
Added page "Edge Research — What We Kept vs Rejected (v3)" to `docs/build_architecture.py`
(lengthy-candle boost, rejected ideas, self-learning loop). PDF rebuilt.

## Pushed
Commit `8f26bba` → github.com/praveen330/NexaQuant (main). Secret scan clean (placeholders only);
`champion.json`/`health.json`/M5-M15 parquets gitignored.

## Deployment status (Oracle VM `nexabot`)
- Old setup script reached [4/6] then stopped. Guided user: `git pull` → re-export MT5 creds →
  re-run `setup_oracle.sh` (idempotent; now finishes 7 stages incl. weekly loop).
- User **rotated the MT5 password** (old one treated as compromised). Good.

## Strategy research backlog (verified sources — to test under the same gate)
TIER 1 (test next): (1) **Volatility-targeting overlay** (AQR/AlphaArchitect: Sharpe 0.40→0.48-0.51),
(2) **Risk-managed momentum** filter (avoid momentum crashes), (3) multi-lookback TSM confirmation.
TIER 2 (AI, gated): (4) wire **meta-label P(win)** as `confidence_source: "ai"` once it clears CPCV;
(5) trend-scanning labels. TIER 3 (research-only, fragile): DRL sizing, transformer/TFT forecasting.
STRUCTURAL: multi-edge portfolio (mean-reversion sleeve for range regimes) for smoother equity.
Recommended first build: **volatility-targeting overlay** (best-verified, small change).

## Next steps
1. Finish Oracle VM setup (git pull + re-run script) → confirm bot service + weekly timer active.
2. Build + back-test the **volatility-targeting overlay** on BTC H4 (keep only if it passes the gate).
3. 30-day paper on BTC H4 cent account before any live switch.
